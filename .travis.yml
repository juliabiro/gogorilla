language: go
go:
  - "1.15.2"

before_install:
- sudo apt-get install xorg-dev libgl1-mesa-dev

jobs:
  include:
    - stage: build
      script:
        go build main.go
    - stage: test
      script: go test ./gorilla


